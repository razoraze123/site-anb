---
import DashboardCore from '../components/admin/DashboardCore.astro';
import ViewActualites from '../components/admin/ViewActualites.astro';
import ViewEvenements from '../components/admin/ViewEvenements.astro';
import ViewInscriptions from '../components/admin/ViewInscriptions.astro';
import ViewMessages from '../components/admin/ViewMessages.astro';

export const prerender = false;

// Le middleware garantit déjà le rôle super_admin sur cette route ; on relit
// la session pour disposer du nom (rédacteur des actualités notamment).
const user = await Astro.session?.get('user');
if (!user) {
  return Astro.redirect('/connexion');
}
---
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>Espace ANB — Super Admin</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@600;700;800&family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <style is:global>
    :root {
      --color-cream: #F8F4EC;
      --color-green: #176B4D;
      --color-orange: #E97824;
      --color-dark: #1F2925;
      --color-sand: #E8D8BF;
      --color-body-text: #3f4a45;
      --color-muted-text: #5a655f;
      --font-display: 'Unbounded', sans-serif;
      --font-body: 'Manrope', sans-serif;
    }
    * { box-sizing: border-box; }
    html, body { margin: 0; padding: 0; }
    body {
      font-family: var(--font-body);
      background: var(--color-cream);
      color: var(--color-dark);
      -webkit-font-smoothing: antialiased;
    }
    a { text-decoration: none; }
    button, input, textarea, select { font-family: inherit; }
    ::selection { background: var(--color-orange); color: #FFFFFF; }
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-thumb { background: rgba(31,41,37,0.15); border-radius: 8px; }
    
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(16px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .fade-in { animation: fadeInUp 0.4s ease both; }
    .hidden { display: none !important; }
    .active-nav-btn {
      color: var(--color-green) !important;
      background: rgba(23, 107, 77, 0.09) !important;
      font-weight: 700 !important;
    }
    
    .sidebar-link {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 16px;
      border-radius: 12px;
      font-size: 14.5px;
      font-weight: 600;
      color: var(--color-body-text);
      cursor: pointer;
      transition: all 0.15s ease;
      background: transparent;
      border: none;
      width: 100%;
      text-align: left;
    }
    .sidebar-link:hover {
      background: rgba(31, 41, 37, 0.04);
      color: var(--color-dark);
    }
    
    .nav-desktop-only { display: flex; }
    .nav-mobile-only { display: none; }
    @media (max-width: 980px) {
      .nav-desktop-only { display: none; }
      .nav-mobile-only { display: flex; }
    }
  </style>
</head>
<body>

  <!-- APP SHELL -->
  <div id="app-shell-view" style="display:flex; min-height:100vh;">
    
    <!-- SIDEBAR DESKTOP -->
    <aside class="nav-desktop-only" style="width:260px; flex-shrink:0; background:#FFFFFF; border-right:1px solid rgba(31,41,37,0.08); display:flex; flex-direction:column; height:100vh; position:sticky; top:0;">
      <div style="padding:22px 20px; display:flex; align-items:center; gap:12px; border-bottom:1px solid rgba(31,41,37,0.06);">
        <div style="width:38px; height:38px; border-radius:11px; background:#176B4D; display:flex; align-items:center; justify-content:center; flex-shrink:0;">
          <span style="font-family:'Unbounded',sans-serif; font-weight:700; font-size:12px; color:#FFFFFF;">ANB</span>
        </div>
        <div style="flex:1; min-width:0;">
          <div style="font-family:'Unbounded',sans-serif; font-weight:700; font-size:14.5px; color:#176B4D; white-space:nowrap;">Espace ANB</div>
          <div style="font-size:11px; color:#5a655f; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;">Super Admin</div>
        </div>
      </div>
      
      <nav id="desktop-sidebar-nav" style="flex:1; overflow-y:auto; padding:16px 12px; display:flex; flex-direction:column; gap:4px;">
        <!-- Dynamically rendered -->
      </nav>
      
      <div style="padding:16px; border-top:1px solid rgba(31,41,37,0.06); display:flex; align-items:center; gap:12px;">
        <div style="width:38px; height:38px; border-radius:50%; background:linear-gradient(150deg,#176B4D,#E97824); flex-shrink:0;"></div>
        <div style="flex:1; min-width:0;">
          <div id="sidebar-user-name" style="font-size:13.5px; font-weight:700; color:#1F2925; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;">—</div>
          <div id="sidebar-user-role" style="font-size:11.5px; color:#5a655f;">—</div>
        </div>
        <button class="logout-btn" aria-label="Déconnexion" style="width:34px; height:34px; border-radius:10px; background:#F8F4EC; border:none; display:flex; align-items:center; justify-content:center; cursor:pointer; flex-shrink:0;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#1F2925" stroke-width="1.8"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"></path><path d="M16 17l5-5-5-5"></path><path d="M21 12H9"></path></svg>
        </button>
      </div>
    </aside>

    <!-- MOBILE DRAWER -->
    <div id="mobile-sidebar-drawer" class="hidden" style="position:fixed; inset:0; z-index:300; background:#FFFFFF; display:flex; flex-direction:column; overflow-y:auto;">
      <div style="padding:20px; display:flex; align-items:center; justify-content:space-between; border-bottom:1px solid rgba(31,41,37,0.06);">
        <div style="display:flex; align-items:center; gap:12px;">
          <div style="width:38px; height:38px; border-radius:11px; background:#176B4D; display:flex; align-items:center; justify-content:center;">
            <span style="font-family:'Unbounded',sans-serif; font-weight:700; font-size:12px; color:#FFFFFF;">ANB</span>
          </div>
          <div style="font-family:'Unbounded',sans-serif; font-weight:700; font-size:14.5px; color:#176B4D; white-space:nowrap;">Espace ANB</div>
        </div>
        <button id="close-mobile-drawer" aria-label="Fermer" style="width:40px; height:40px; border-radius:10px; background:#F8F4EC; border:none; display:flex; align-items:center; justify-content:center; cursor:pointer;">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1F2925" stroke-width="2"><line x1="5" y1="5" x2="19" y2="19"></line><line x1="19" y1="5" x2="5" y2="19"></line></svg>
        </button>
      </div>
      <nav id="mobile-sidebar-nav" style="flex:1; padding:16px; display:flex; flex-direction:column; gap:4px;">
        <!-- Dynamically rendered -->
      </nav>
      <div style="padding:16px; border-top:1px solid rgba(31,41,37,0.06);">
        <button class="logout-btn" style="width:100%; background:#F8F4EC; border:none; padding:14px; border-radius:12px; font-weight:700; font-size:14px; cursor:pointer; color:#1F2925;">Se déconnecter</button>
      </div>
    </div>

    <!-- MAIN COLUMN -->
    <div style="flex:1; min-width:0; display:flex; flex-direction:column;">
      
      <!-- TOP BAR -->
      <div style="position:sticky; top:0; z-index:50; background:rgba(248,244,236,0.92); backdrop-filter:blur(8px); border-bottom:1px solid rgba(31,41,37,0.08); padding:16px clamp(16px,3vw,32px); display:flex; align-items:center; gap:16px;">
        <button id="open-mobile-drawer" class="nav-mobile-only" aria-label="Menu" style="width:40px; height:40px; border-radius:10px; background:#FFFFFF; border:1px solid rgba(31,41,37,0.1); display:flex; align-items:center; justify-content:center; cursor:pointer; flex-shrink:0;">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1F2925" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
        </button>
        <div style="flex:1; min-width:0;">
          <div style="font-size:12px; color:#5a655f; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;">Espace ANB / <span id="breadcrumb-page-label">Vue d'ensemble</span></div>
          <div id="title-page-label" style="font-size:17px; font-weight:800; color:#1F2925; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;">Vue d'ensemble</div>
        </div>
        <div class="nav-desktop-only" style="flex:1; max-width:340px; position:relative;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#5a655f" stroke-width="1.8" style="position:absolute; left:14px; top:50%; transform:translateY(-50%);"><circle cx="11" cy="11" r="7"></circle><line x1="21" y1="21" x2="16.5" y2="16.5"></line></svg>
          <input id="global-search-input" placeholder="Recherche globale…" style="width:100%; padding:11px 16px 11px 40px; border-radius:999px; border:1.5px solid #e3dccb; background:#FFFFFF; font-size:13.5px;" />
        </div>
        <button aria-label="Notifications" style="position:relative; width:40px; height:40px; border-radius:10px; background:#FFFFFF; border:1px solid rgba(31,41,37,0.08); display:flex; align-items:center; justify-content:center; cursor:pointer; flex-shrink:0;">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1F2925" stroke-width="1.7"><path d="M18 8a6 6 0 10-12 0c0 7-3 9-3 9h18s-3-2-3-9"></path><path d="M13.7 21a2 2 0 01-3.4 0"></path></svg>
          <span style="position:absolute; top:6px; right:7px; width:8px; height:8px; border-radius:50%; background:#E97824; border:1.5px solid #FFFFFF;"></span>
        </button>
        <button class="nav-desktop-only" aria-label="Aide" style="width:40px; height:40px; border-radius:10px; background:#FFFFFF; border:1px solid rgba(31,41,37,0.08); display:flex; align-items:center; justify-content:center; cursor:pointer; flex-shrink:0;">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1F2925" stroke-width="1.7"><circle cx="12" cy="12" r="9"></circle><path d="M9.5 9.5a2.5 2.5 0 014.9.8c0 1.7-2.4 1.8-2.4 3.7"></path><circle cx="12" cy="17.2" r="0.4" fill="#1F2925"></circle></svg>
        </button>
        <div style="width:38px; height:38px; border-radius:50%; background:linear-gradient(150deg,#176B4D,#E97824); flex-shrink:0;"></div>
      </div>

      <!-- VIEWS CONTAINER -->
      <div style="flex:1; padding:clamp(18px,3vw,32px);">
        
        <!-- OVERVIEW VIEW — dashboard partagé avec /admin, voir
             src/components/admin/DashboardCore.astro + lib/dashboardCore.js.
             role="super_admin" ajoute la section Gouvernance & plateforme. -->
        <section id="view-overview" class="hidden fade-in">
          <DashboardCore userName={user.nom} role="super_admin" />
        </section>

        <!-- GESTION DU SITE — vues partagées avec /admin -->
        <ViewActualites authorName={user.nom} />
        <ViewEvenements />
        <ViewInscriptions />
        <ViewMessages />

        <!-- UTILISATEURS ET ROLES VIEW -->
        <section id="view-users" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <div style="display:flex; flex-wrap:wrap; justify-content:space-between; align-items:center; gap:14px;">
              <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Utilisateurs et rôles</h1>
              <button id="toggle-invite-btn" style="background:#E97824; color:#FFFFFF; border:none; padding:12px 22px; border-radius:999px; font-weight:700; font-size:14px; cursor:pointer;">Inviter un utilisateur</button>
            </div>

            <div id="invite-user-box" class="hidden" style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05); display:flex; flex-direction:column; gap:14px; max-width:520px;">
              <h3 style="font-size:16px; font-weight:700; color:#1F2925; margin:0;">Inviter un utilisateur</h3>
              <input id="invite-form-name" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" placeholder="Nom complet" />
              <input id="invite-form-email" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" placeholder="E-mail" />
              <select id="invite-form-role" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px; background: transparent;">
                <option>Admin</option>
                <option>Super Admin</option>
                <option>Éditeur / Bénévole</option>
              </select>
              <input id="invite-form-périmètre" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" placeholder="Périmètre d'accès (ex : Actualités, Événements)" />
              <textarea id="invite-form-msg" rows="2" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px; resize:vertical;" placeholder="Message d'invitation (facultatif)"></textarea>
              <button id="invite-user-submit-btn" style="align-self:flex-start; background:#176B4D; color:#FFFFFF; border:none; padding:12px 22px; border-radius:999px; font-weight:700; font-size:14px; cursor:pointer;">Envoyer l'invitation</button>
              <div id="invite-success-alert" class="hidden" style="background:rgba(23,107,77,0.08); color:#176B4D; padding:12px 16px; border-radius:10px; font-size:13px; font-weight:700;">✓ Invitation envoyée.</div>
            </div>

            <div style="display:grid; grid-template-columns:1fr 1fr; gap:20px; align-items:start; flex-wrap:wrap;">
              <div style="background:#FFFFFF; border-radius:20px; padding:8px; box-shadow:0 8px 24px rgba(31,41,37,0.05); min-width:0;" id="users-list-container">
                <!-- Dynamic -->
              </div>

              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05); min-width:0;" id="user-detail-box">
                <!-- Dynamic -->
              </div>
            </div>

            <div style="background:#FFFFFF; border-radius:20px; padding:26px; box-shadow:0 8px 24px rgba(31,41,37,0.05); overflow-x:auto;">
              <h3 style="font-size:16px; font-weight:700; color:#1F2925; margin:0 0 18px;">Matrice de permissions</h3>
              <div style="min-width:560px;">
                <div style="display:grid; grid-template-columns:1.6fr 1fr 1fr 1fr; gap:8px; padding-bottom:10px; border-bottom:2px solid rgba(31,41,37,0.08); margin-bottom:8px;">
                  <div></div>
                  <div style="font-size:12.5px; font-weight:700; color:#176B4D; text-align:center;">Super Admin</div>
                  <div style="font-size:12.5px; font-weight:700; color:#E97824; text-align:center;">Admin</div>
                  <div style="font-size:12.5px; font-weight:700; color:#5a655f; text-align:center;">Éditeur</div>
                </div>
                <div id="permission-matrix-rows">
                  <!-- Dynamic -->
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- CONTENUS ET VALIDATIONS VIEW -->
        <section id="view-contenus" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Contenus et validations</h1>
            <div id="validations-columns" style="display:grid; grid-template-columns:1fr 1.2fr; gap:20px; align-items:start; flex-wrap:wrap;">
              <div style="background:#FFFFFF; border-radius:20px; padding:8px; box-shadow:0 8px 24px rgba(31,41,37,0.05); min-width:0;" id="validations-queue-list">
                <!-- Dynamic -->
              </div>
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05); min-width:0;" id="validation-detail-box">
                <!-- Dynamic -->
              </div>
            </div>
            <div id="validations-empty-state" class="hidden" style="background:#FFFFFF; border-radius:20px; padding:70px 24px; box-shadow:0 8px 24px rgba(31,41,37,0.05); display:flex; flex-direction:column; align-items:center; text-align:center; gap:14px;">
              <svg width="34" height="34" viewBox="0 0 24 24" fill="none" stroke="#9aa39c" stroke-width="1.6"><path d="M9 12l2 2 4-4"></path><circle cx="12" cy="12" r="9"></circle></svg>
              <div style="font-size:15.5px; font-weight:700; color:#1F2925;">Aucune validation en attente</div>
              <div style="font-size:13.5px; color:#5a655f; max-width:360px;">Les contenus soumis par les éditeurs apparaîtront ici lorsqu'ils seront prêts à être examinés.</div>
            </div>
          </div>
        </section>

        <!-- DONNEES ET EXPORTS VIEW -->
        <section id="view-donnees" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Données et exports</h1>
            <div style="background:#FFFFFF; border-radius:20px; padding:26px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
              <h3 style="font-size:15.5px; font-weight:700; color:#1F2925; margin:0 0 16px;">Générer un export sécurisé</h3>
              <div style="display:flex; flex-direction:column; gap:8px; margin-bottom:16px;">
                <label style="display:flex; align-items:center; gap:10px; font-size:13.5px; color:#1F2925;"><input type="checkbox" id="export-check-recensement" checked />Recensement (adhésions)</label>
                <label style="display:flex; align-items:center; gap:10px; font-size:13.5px; color:#1F2925;"><input type="checkbox" id="export-check-inscriptions" checked />Inscriptions aux événements</label>
                <label style="display:flex; align-items:center; gap:10px; font-size:13.5px; color:#1F2925;"><input type="checkbox" id="export-check-messages" />Messages de contact</label>
              </div>
              <div style="display:flex; gap:14px; flex-wrap:wrap; margin-bottom:16px;">
                <select id="export-period" style="flex:1; min-width:180px; padding:11px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:13.5px; background: transparent;">
                  <option value="30">30 derniers jours</option>
                  <option value="90">3 derniers mois</option>
                  <option value="">Tout l'historique</option>
                </select>
                <input id="export-reason" style="flex:2; min-width:220px; padding:11px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:13.5px;" placeholder="Raison de l'export (obligatoire)" />
              </div>
              <button id="generate-export-btn" style="background:#E97824; color:#FFFFFF; border:none; padding:12px 24px; border-radius:999px; font-weight:700; font-size:14px; cursor:pointer;">Générer l'export sécurisé</button>
              
              <div id="export-confirm-box" class="hidden" style="margin-top:16px; background:#F8F4EC; border-radius:14px; padding:18px; display:flex; flex-direction:column; gap:10px;">
                <div style="font-size:13.5px; color:#1F2925; font-weight:600;">Confirmez-vous cet export ? Il sera tracé dans le journal d'activité et limité aux champs strictement nécessaires.</div>
                <div style="display:flex; gap:10px;">
                  <button id="confirm-export-action" style="background:#176B4D; color:#FFFFFF; border:none; padding:10px 20px; border-radius:999px; font-weight:700; font-size:13px; cursor:pointer;">Confirmer l'export</button>
                  <button id="cancel-export-action" style="background:#FFFFFF; color:#1F2925; border:1px solid rgba(31,41,37,0.15); padding:10px 20px; border-radius:999px; font-weight:700; font-size:13px; cursor:pointer;">Annuler</button>
                </div>
              </div>
              <div id="export-success-alert" class="hidden" style="margin-top:16px; background:rgba(23,107,77,0.08); color:#176B4D; padding:12px 16px; border-radius:10px; font-size:13px; font-weight:700;">✓ Export généré et tracé dans le journal d'activité.</div>
            </div>

            <div style="background:#FFFFFF; border-radius:20px; padding:26px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
              <h3 style="font-size:15.5px; font-weight:700; color:#1F2925; margin:0 0 10px;">Demandes liées aux données personnelles</h3>
              <div style="font-size:13px; color:#5a655f; line-height:1.7;">Ces demandes (accès, suppression, rectification) ne sont pas encore suivies dans l'outil — elles arrivent par e-mail à <a href="mailto:anbordeaux33@outlook.fr" style="color:#176B4D;">anbordeaux33@outlook.fr</a> et sont traitées manuellement.</div>
            </div>
          </div>
        </section>

        <!-- JOURNAL D'ACTIVITE VIEW -->
        <section id="view-journal" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Journal d'activité</h1>
            <p style="font-size:13px; color:#5a655f; margin:0;">Journal en lecture seule — chaque action sensible y est enregistrée automatiquement.</p>
            <div style="display:flex; gap:8px; flex-wrap:wrap;" class="journal-tab-container">
              <!-- User filters tabs -->
            </div>
            <div style="background:#FFFFFF; border-radius:20px; padding:8px; box-shadow:0 8px 24px rgba(31,41,37,0.05);" id="journal-list-container">
              <!-- Logs items -->
            </div>
          </div>
        </section>

        <!-- REGLAGES DU SITE VIEW -->
        <section id="view-reglages" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Réglages du site</h1>
            <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(320px,1fr)); gap:20px;">
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <h3 style="font-size:15.5px; font-weight:700; color:#1F2925; margin:0 0 16px;">Identité de l'association</h3>
                <div style="display:flex; flex-direction:column; gap:14px;">
                  <div><label style="font-size:12.5px; font-weight:700; color:#1F2925; display:block; margin-bottom:4px;">Nom</label><input id="settings-identite-nom" style="width:100%; padding:10px 12px; border-radius:9px; border:1.5px solid #e3dccb; font-size:13.5px;" /><div style="font-size:11.5px; color:#9aa39c; margin-top:4px;">Enregistré en base — pas encore affiché automatiquement sur le site public.</div></div>
                  <div><label style="font-size:12.5px; font-weight:700; color:#1F2925; display:block; margin-bottom:4px;">Slogan</label><input id="settings-identite-slogan" style="width:100%; padding:10px 12px; border-radius:9px; border:1.5px solid #e3dccb; font-size:13.5px;" /></div>
                  <div><label style="font-size:12.5px; font-weight:700; color:#1F2925; display:block; margin-bottom:4px;">E-mail officiel</label><input id="settings-identite-email" style="width:100%; padding:10px 12px; border-radius:9px; border:1.5px solid #e3dccb; font-size:13.5px;" /></div>
                  <div><label style="font-size:12.5px; font-weight:700; color:#1F2925; display:block; margin-bottom:4px;">Téléphone</label><input id="settings-identite-telephone" style="width:100%; padding:10px 12px; border-radius:9px; border:1.5px solid #e3dccb; font-size:13.5px;" /></div>
                </div>
                <button id="save-identite-btn" style="margin-top:16px; background:#176B4D; color:#FFFFFF; border:none; padding:10px 20px; border-radius:999px; font-weight:700; font-size:13px; cursor:pointer;">Enregistrer</button>
              </div>
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <h3 style="font-size:15.5px; font-weight:700; color:#1F2925; margin:0 0 16px;">Navigation et pages</h3>
                <div style="font-size:13px; color:#5a655f; line-height:1.7;">Menus, pied de page, pages principales et redirections du site public.<br/><span style="font-size:11.5px; color:#9aa39c;">Pas encore disponible — aucune structure de menu éditable n'existe côté serveur.</span></div>
              </div>
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <h3 style="font-size:15.5px; font-weight:700; color:#1F2925; margin:0 0 16px;">Référencement</h3>
                <div style="display:flex; flex-direction:column; gap:14px;">
                  <div><label style="font-size:12.5px; font-weight:700; color:#1F2925; display:block; margin-bottom:4px;">Titre du site</label><input id="settings-seo-titre" style="width:100%; padding:10px 12px; border-radius:9px; border:1.5px solid #e3dccb; font-size:13.5px;" /></div>
                  <div><label style="font-size:12.5px; font-weight:700; color:#1F2925; display:block; margin-bottom:4px;">Description SEO</label><textarea id="settings-seo-description" rows="2" style="width:100%; padding:10px 12px; border-radius:9px; border:1.5px solid #e3dccb; font-size:13.5px; resize:vertical;"></textarea><div style="font-size:11.5px; color:#9aa39c; margin-top:4px;">Enregistré en base — pas encore lu par les balises SEO des pages publiques.</div></div>
                </div>
                <button id="save-seo-btn" style="margin-top:16px; background:#176B4D; color:#FFFFFF; border:none; padding:10px 20px; border-radius:999px; font-weight:700; font-size:13px; cursor:pointer;">Enregistrer</button>
              </div>
            </div>
          </div>
        </section>

        <!-- INTEGRATIONS VIEW -->
        <section id="view-integrations" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Intégrations</h1>
            <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:18px;" id="integrations-list-container">
              <!-- integrations list dynamically rendered -->
            </div>
          </div>
        </section>

        <!-- SECURITE VIEW -->
        <section id="view-securite" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Sécurité</h1>
            <div style="background:#176B4D; border-radius:20px; padding:24px; color:#FFFFFF; display:flex; align-items:center; gap:14px;">
              <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" stroke-width="1.6"><path d="M12 3l7 3v6c0 5-3 8-7 9-4-1-7-4-7-9V6z"></path><path d="M9.5 12l1.8 1.8 3.2-3.6"></path></svg>
              <div style="font-size:16px; font-weight:700;">Votre espace est protégé</div>
            </div>
            <div style="background:rgba(233,120,36,0.1); color:#B75E12; padding:14px 18px; border-radius:12px; font-size:13.5px; font-weight:600;">Activez la double authentification pour tous les comptes Admin et Super Admin.</div>
            <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(300px,1fr)); gap:20px;">
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:16px;">
                  <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0;">Double authentification</h3>
                  <button id="twofa-toggle-btn" style="width:46px; height:26px; border-radius:999px; background:#176B4D; border:none; cursor:pointer; position:relative;">
                    <span id="twofa-toggle-indicator" style="position:absolute; top:3px; left:23px; width:20px; height:20px; border-radius:50%; background:#FFFFFF; transition:left 0.2s;"></span>
                  </button>
                </div>
                <div id="twofa-status-label" style="font-size:13px; color:#5a655f;">Activée pour votre compte.</div>
              </div>
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0 0 14px;">Politique de mot de passe</h3>
                <div style="font-size:13px; color:#5a655f; line-height:1.7;">Minimum 12 caractères, renouvellement conseillé tous les 6 mois, aucun mot de passe partagé entre comptes.</div>
              </div>
            </div>
            
            <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
              <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:16px;">
                <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0;">Sessions actives</h3>
                <button id="disconnect-all-btn" style="background:rgba(177,69,36,0.08); color:#B14524; border:none; padding:9px 18px; border-radius:999px; font-weight:700; font-size:12.5px; cursor:pointer;">Déconnecter toutes les sessions</button>
              </div>
              <div id="active-sessions-list">
                <!-- dynamic sessions -->
              </div>
            </div>
          </div>
        </section>

        <!-- SAUVEGARDES VIEW -->
        <section id="view-sauvegardes" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Sauvegardes</h1>
            <div style="background:#FFFFFF; border-radius:20px; padding:26px; box-shadow:0 8px 24px rgba(31,41,37,0.05); display:flex; flex-direction:column; gap:14px; max-width:640px;">
              <div style="font-size:14px; color:#1F2925; line-height:1.7;">La base de données (Cloudflare D1) sauvegarde en continu son historique. Pas de système de sauvegarde maison ici : la restauration se fait directement via l'outil <strong>Time Travel</strong> de Cloudflare, jusqu'à 30 jours en arrière.</div>
              <a href="https://developers.cloudflare.com/d1/reference/time-travel/" target="_blank" rel="noopener" style="align-self:flex-start; background:#176B4D; color:#FFFFFF; border:none; padding:12px 22px; border-radius:999px; font-weight:700; font-size:14px; cursor:pointer;">Voir la procédure Time Travel ↗</a>
              <div style="font-size:12.5px; color:#9aa39c;">Commande de restauration : <code>wrangler d1 time-travel restore anb-db --timestamp=...</code></div>
            </div>
          </div>
        </section>

        <!-- MENTIONS ET CLAUSES VIEW -->
        <section id="view-mentions" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px; max-width:720px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Mentions légales et confidentialité</h1>
            <div id="legal-sections-container" style="display:flex; flex-direction:column; gap:16px;">
              <!-- dynamic legal categories -->
            </div>
          </div>
        </section>

        <!-- STATISTIQUES GLOBAL VIEW -->
        <section id="view-stats" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px;">
            <div style="display:flex; flex-wrap:wrap; justify-content:space-between; align-items:center; gap:14px;">
              <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Statistiques globales</h1>
              <div style="display:flex; gap:8px; flex-wrap:wrap;" class="stats-period-container">
                <!-- Period options -->
              </div>
            </div>
            <p style="font-size:12.5px; color:#9aa39c; margin:0;">Basé uniquement sur les données de la plateforme — aucun outil de mesure d'audience (pages vues, visiteurs) n'est branché sur le site pour l'instant.</p>
            <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(180px,1fr)); gap:16px;">
              <div style="background:#FFFFFF; border-radius:16px; padding:20px; box-shadow:0 8px 24px rgba(31,41,37,0.05);"><div id="stat-period-inscriptions" style="font-size:22px; font-weight:800; color:#176B4D;">—</div><div style="font-size:13px; color:#5a655f;">Inscriptions aux événements</div></div>
              <div style="background:#FFFFFF; border-radius:16px; padding:20px; box-shadow:0 8px 24px rgba(31,41,37,0.05);"><div id="stat-period-adhesions" style="font-size:22px; font-weight:800; color:#E97824;">—</div><div style="font-size:13px; color:#5a655f;">Nouvelles demandes d'adhésion</div></div>
              <div style="background:#FFFFFF; border-radius:16px; padding:20px; box-shadow:0 8px 24px rgba(31,41,37,0.05);"><div id="stat-period-membres" style="font-size:22px; font-weight:800; color:#176B4D;">—</div><div style="font-size:13px; color:#5a655f;">Personnes recensées (total)</div></div>
              <div style="background:#FFFFFF; border-radius:16px; padding:20px; box-shadow:0 8px 24px rgba(31,41,37,0.05);"><div id="stat-period-evenements" style="font-size:22px; font-weight:800; color:#E97824;">—</div><div style="font-size:13px; color:#5a655f;">Événements à venir</div></div>
            </div>
            <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:20px;">
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0 0 14px;">Prochains événements</h3>
                <div id="top-pages-list"></div>
              </div>
              <div style="background:#FFFFFF; border-radius:20px; padding:24px; box-shadow:0 8px 24px rgba(31,41,37,0.05);">
                <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0 0 14px;">Derniers articles publiés</h3>
                <div id="top-articles-list"></div>
              </div>
            </div>
          </div>
        </section>

        <!-- MON PROFIL VIEW -->
        <section id="view-profil" class="hidden fade-in">
          <div style="display:flex; flex-direction:column; gap:20px; max-width:600px;">
            <h1 style="font-size:22px; font-weight:800; color:#1F2925; margin:0;">Mon profil</h1>
            <div style="background:#FFFFFF; border-radius:20px; padding:26px; box-shadow:0 8px 24px rgba(31,41,37,0.05); display:flex; flex-direction:column; gap:16px;">
              <div style="display:flex; align-items:center; gap:16px;">
                <div style="width:56px; height:56px; border-radius:50%; background:linear-gradient(150deg,#176B4D,#E97824);"></div>
                <div>
                  <div id="profile-header-name" style="font-size:16px; font-weight:700; color:#1F2925;">—</div>
                  <div id="profile-header-role" style="font-size:13px; color:#5a655f;">—</div>
                </div>
              </div>
              <div>
                <label style="font-size:13px; font-weight:700; color:#1F2925; display:block; margin-bottom:6px;">Nom complet</label>
                <input id="profile-form-name" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" />
              </div>
              <div>
                <label style="font-size:13px; font-weight:700; color:#1F2925; display:block; margin-bottom:6px;">E-mail</label>
                <input id="profile-form-email" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" placeholder="vous@anb-bordeaux.fr" />
              </div>
              <div>
                <label style="font-size:13px; font-weight:700; color:#1F2925; display:block; margin-bottom:6px;">Rôle</label>
                <input id="profile-form-role" readonly style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px; color:#9aa39c;" />
              </div>
              <div>
                <label style="font-size:13px; font-weight:700; color:#1F2925; display:block; margin-bottom:6px;">Nouveau mot de passe</label>
                <div style="position:relative;">
                  <input id="profile-form-password" type="password" style="width:100%; padding:12px 44px 12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" placeholder="Laisser vide pour ne pas changer" />
                  <button type="button" class="toggle-password-visibility-btn" data-target="profile-form-password" aria-label="Afficher le mot de passe" style="position:absolute; top:50%; right:10px; transform:translateY(-50%); background:none; border:none; cursor:pointer; padding:4px; color:#5a655f; display:flex;">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z"></path><circle cx="12" cy="12" r="3"></circle></svg>
                  </button>
                </div>
              </div>
              <div>
                <label style="font-size:13px; font-weight:700; color:#1F2925; display:block; margin-bottom:6px;">Confirmer le nouveau mot de passe</label>
                <div style="position:relative;">
                  <input id="profile-form-password-confirm" type="password" style="width:100%; padding:12px 44px 12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:14px;" placeholder="Ressaisir le mot de passe" />
                  <button type="button" class="toggle-password-visibility-btn" data-target="profile-form-password-confirm" aria-label="Afficher le mot de passe" style="position:absolute; top:50%; right:10px; transform:translateY(-50%); background:none; border:none; cursor:pointer; padding:4px; color:#5a655f; display:flex;">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z"></path><circle cx="12" cy="12" r="3"></circle></svg>
                  </button>
                </div>
              </div>
              <button id="save-profile-btn" style="align-self:flex-start; background:#176B4D; color:#FFFFFF; border:none; padding:12px 24px; border-radius:999px; font-weight:700; font-size:14px; cursor:pointer;">Enregistrer</button>
            </div>
          </div>
        </section>

      </div>
    </div>
  </div>

  <script>
    import { createAdminContent } from '../lib/adminContent.js';
    import { createDashboardCore } from '../lib/dashboardCore.js';

    // Vues Actualités / Événements / Inscriptions / Messages : exactement
    // les mêmes que dans /admin (markup dans src/components/admin/,
    // logique dans lib/adminContent.js) — un super-admin est un admin,
    // même dans son propre espace.
    const content = createAdminContent({
      goPage: (page) => goPage(page),
      getBadgeHtml: (label, tone) => getBadgeHtml(label, tone),
      onViewRegistrants: (eventId) => {
        content.state.pendingInscriptionsEventId = eventId;
        goPage('inscriptions');
      },
    });

    // Dashboard (Vue d'ensemble) : même Dashboard Core que /admin, plus la
    // section Gouvernance & plateforme (role: 'super_admin').
    const dashboard = createDashboardCore({
      goPage: (page) => goPage(page),
      getBadgeHtml: (label, tone) => getBadgeHtml(label, tone),
      role: 'super_admin',
    });

    // State management
    const state = {
      user: 'super_admin',
      page: 'overview',
      userSelectedIdx: 0,
      inviteOpen: false,
      validationSelectedIdx: 0,
      exportConfirmOpen: false,
      twoFA: true,
      backupRunning: false,
      restoreOpen: false,
      statsPeriod: '30 jours',
      journalUserFilter: 'Tous',
      currentUserId: null,
    };

    const navigationItems = [
      { key: 'overview', label: "Vue d'ensemble" },
      { type: 'header', label: 'Gestion du site' },
      { key: 'actualites', label: 'Actualités' },
      { key: 'evenements', label: 'Événements' },
      { key: 'inscriptions', label: 'Inscriptions' },
      { key: 'messages', label: 'Messages' },
      { type: 'header', label: 'Gouvernance' },
      { key: 'users', label: 'Utilisateurs & rôles' },
      { key: 'contenus', label: 'Contenus & validations' },
      { key: 'donnees', label: 'Données & exports' },
      { key: 'journal', label: "Journal d'activité" },
      { key: 'reglages', label: 'Réglages du site' },
      { key: 'integrations', label: 'Intégrations' },
      { key: 'securite', label: 'Sécurité' },
      { key: 'sauvegardes', label: 'Sauvegardes' },
      { key: 'mentions', label: 'Mentions & RGPD' },
      { key: 'stats', label: 'Statistiques globales' },
      { type: 'separator' },
      { key: 'profil', label: 'Mon profil' }
    ];

    const svgMap = {
      overview: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><rect x="3" y="3" width="8" height="8" rx="2"></rect><rect x="13" y="3" width="8" height="8" rx="2"></rect><rect x="3" y="13" width="8" height="8" rx="2"></rect><rect x="13" y="13" width="8" height="8" rx="2"></rect></svg>`,
      actualites: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><rect x="3" y="4" width="18" height="16" rx="2"></rect><line x1="7" y1="9" x2="17" y2="9"></line><line x1="7" y1="13" x2="17" y2="13"></line><line x1="7" y1="17" x2="12" y2="17"></line></svg>`,
      evenements: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><rect x="3" y="5" width="18" height="16" rx="2"></rect><line x1="3" y1="10" x2="21" y2="10"></line><line x1="8" y1="3" x2="8" y2="7"></line><line x1="16" y1="3" x2="16" y2="7"></line></svg>`,
      inscriptions: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M9 12l2 2 4-4"></path><circle cx="12" cy="12" r="9"></circle></svg>`,
      messages: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><rect x="3" y="5" width="18" height="14" rx="2"></rect><path d="M3 6l9 6 9-6"></path></svg>`,
      users: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><circle cx="9" cy="8" r="3.2"></circle><circle cx="17" cy="9" r="2.4"></circle><path d="M3 20c0-3.4 2.7-5.8 6-5.8s6 2.4 6 5.8"></path></svg>`,
      contenus: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M9 12l2 2 4-4"></path><circle cx="12" cy="12" r="9"></circle></svg>`,
      donnees: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><ellipse cx="12" cy="5.5" rx="8" ry="2.8"></ellipse><path d="M4 5.5V18c0 1.5 3.6 2.8 8 2.8s8-1.3 8-2.8V5.5"></path><path d="M4 12c0 1.5 3.6 2.8 8 2.8s8-1.3 8-2.8"></path></svg>`,
      journal: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><circle cx="12" cy="12" r="9"></circle><path d="M12 7v5l3.5 2"></path></svg>`,
      reglages: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 13a7.6 7.6 0 000-2l2-1.5-2-3.4-2.3.9a7.6 7.6 0 00-1.7-1L15 3.5h-4l-.4 2.5a7.6 7.6 0 00-1.7 1l-2.3-.9-2 3.4L6.6 11a7.6 7.6 0 000 2l-2 1.5 2 3.4 2.3-.9a7.6 7.6 0 001.7 1l.4 2.5h4l.4-2.5a7.6 7.6 0 001.7-1l2.3.9 2-3.4z"></path></svg>`,
      integrations: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M9 3v5M15 3v5M4 8h16l-1.5 11a2 2 0 01-2 1.8H7.5a2 2 0 01-2-1.8z"></path></svg>`,
      securite: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M12 3l7 3v6c0 5-3 8-7 9-4-1-7-4-7-9V6z"></path><path d="M9.5 12l1.8 1.8 3.2-3.6"></path></svg>`,
      sauvegardes: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M7 17a4 4 0 01-1-7.9 5 5 0 019.6-1.7A4.5 4.5 0 0117 17H7z"></path></svg>`,
      mentions: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M12 3l8 4v5c0 5-3.4 8-8 9-4.6-1-8-4-8-9V7z"></path><line x1="9" y1="11" x2="15" y2="11"></line><line x1="9" y1="14" x2="15" y2="14"></line></svg>`,
      stats: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M4 20V10M12 20V4M20 20v-7"></path></svg>`,
      profil: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><circle cx="12" cy="8" r="3.4"></circle><path d="M5 20c0-3.6 3.1-6 7-6s7 2.4 7 6"></path></svg>`
    };

    // Rempli par loadUsers() depuis GET /api/superadmin/users — plus de données figées.
    let users = [];

    const ROLE_LABELS = { admin: ['Admin', 'o'], super_admin: ['Super Admin', 'g'], editeur: ['Éditeur', 'n'] };
    const ROLE_PERMISSIONS = {
      admin: "Gestion des contenus, événements, médias, messages et demandes. Traitement des inscriptions.",
      super_admin: "Accès complet : utilisateurs et rôles, réglages globaux, exports, journal d'activité.",
      editeur: "Création d'articles, événements et ressources en brouillon uniquement."
    };
    const AVATAR_GRADIENTS = ['linear-gradient(150deg,#176B4D,#E97824)', 'linear-gradient(150deg,#1F2925,#176B4D)', 'linear-gradient(150deg,#E8D8BF,#176B4D)', 'linear-gradient(150deg,#E97824,#1F2925)', 'linear-gradient(150deg,#5a655f,#1F2925)'];

    function decorateUser(u, i) {
      const [roleLabel, roleT] = ROLE_LABELS[u.role] || [u.role, 'n'];
      return {
        id: u.id,
        name: u.nom,
        email: u.email,
        dbRole: u.role,
        dbStatut: u.statut,
        role: roleLabel,
        roleT,
        status: u.statut === 'actif' ? 'Actif' : 'Désactivé',
        statusT: u.statut === 'actif' ? 'g' : 'r',
        last: '—',
        avatarBg: AVATAR_GRADIENTS[i % AVATAR_GRADIENTS.length],
        permissions: ROLE_PERMISSIONS[u.role] || ''
      };
    }

    async function loadUsers() {
      const container = document.getElementById('users-list-container');
      container.innerHTML = '<div style="padding:24px; font-size:13.5px; color:#5a655f;">Chargement…</div>';
      try {
        const res = await fetch('/api/superadmin/users');
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur de chargement.');
        users = data.users.map(decorateUser);
        state.userSelectedIdx = 0;
        renderUsers();
      } catch (err) {
        container.innerHTML = `<div style="padding:24px; font-size:13.5px; color:#B14524;">${err.message}</div>`;
      }
    }

    const permissionMatrix = [
      { label: 'Gestion des contenus (articles, événements, médias)', superadmin: '✓', admin: '✓', editeur: 'Brouillon (articles)' },
      { label: 'Traitement des inscriptions et adhésions', superadmin: '✓', admin: '✓', editeur: '—' },
      { label: 'Messages et demandes de contact', superadmin: '✓', admin: '✓', editeur: '—' },
      { label: 'Statistiques de fréquentation', superadmin: '✓', admin: '✓', editeur: '—' },
      { label: 'Gestion des utilisateurs et rôles', superadmin: '✓', admin: '—', editeur: '—' },
      { label: 'Réglages globaux du site', superadmin: '✓', admin: '—', editeur: '—' },
      { label: 'Exports de données de la plateforme', superadmin: '✓', admin: '—', editeur: '—' },
      { label: 'Sécurité et sauvegardes', superadmin: '✓', admin: '—', editeur: '—' }
    ];

    // Rempli par loadValidations() depuis GET /api/superadmin/validations
    // (actualités au statut 'En attente' — posé par la soumission éditeur,
    // Phase 2, pas encore branchée : cette file peut donc être vide pour
    // l'instant).
    let validationQueue = [];

    async function loadValidations() {
      const container = document.getElementById('validations-queue-list');
      container.innerHTML = '<div style="padding:24px; font-size:13.5px; color:#5a655f;">Chargement…</div>';
      try {
        const res = await fetch('/api/superadmin/validations');
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur de chargement.');
        validationQueue = data.items.map(it => ({
          id: it.id,
          author: it.auteur_nom || 'Auteur inconnu',
          type: 'Article',
          date: formatJournalDate(it.created_at),
          title: it.title,
          status: it.status
        }));
        state.validationSelectedIdx = 0;
        renderValidations();
      } catch (err) {
        container.innerHTML = `<div style="padding:24px; font-size:13.5px; color:#B14524;">${err.message}</div>`;
      }
    }

    // Rempli par loadJournal() depuis GET /api/superadmin/journal.
    let journalLogs = [];

    const JOURNAL_ROLE_T = { Admin: 'o', 'Super Admin': 'g', 'Éditeur': 'n' };
    const MONTHS_FR = ['janvier', 'février', 'mars', 'avril', 'mai', 'juin', 'juillet', 'août', 'septembre', 'octobre', 'novembre', 'décembre'];

    function firstNameFromEmail(email) {
      const local = (email || '').split('@')[0] || '';
      const first = local.split('.')[0] || local;
      return first ? first.charAt(0).toUpperCase() + first.slice(1) : 'Inconnu';
    }

    function formatJournalDate(sqlDate) {
      // "YYYY-MM-DD HH:MM:SS" (UTC, tel que stocké par D1) -> "12 juillet 2026, 14:32"
      if (!sqlDate) return '—';
      const [datePart, timePart] = sqlDate.split(' ');
      const [y, m, d] = (datePart || '').split('-').map(Number);
      const [h, min] = (timePart || '').split(':');
      if (!y || !m || !d) return sqlDate;
      return `${d} ${MONTHS_FR[m - 1]} ${y}, ${h}:${min}`;
    }

    async function loadJournal() {
      const container = document.getElementById('journal-list-container');
      container.innerHTML = '<div style="padding:24px; font-size:13.5px; color:#5a655f;">Chargement…</div>';
      try {
        const res = await fetch('/api/superadmin/journal');
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur de chargement.');
        journalLogs = data.logs.map(l => ({
          date: formatJournalDate(l.created_at),
          user: firstNameFromEmail(l.utilisateur_email),
          role: l.role,
          roleT: JOURNAL_ROLE_T[l.role] || 'n',
          action: l.action,
          detail: l.details,
          ip: l.adresse_ip
        }));
        renderJournal();
      } catch (err) {
        container.innerHTML = `<div style="padding:24px; font-size:13.5px; color:#B14524;">${err.message}</div>`;
      }
    }

    // État honnête : aucun outil tiers n'est réellement branché pour
    // l'instant (à part la réception des formulaires, déjà en D1).
    const integrations = [
      { name: 'Google Maps', desc: 'Affiche la carte sur la page Contact', status: 'À venir', tone: 'n', sync: '—', actionLabel: 'Bientôt' },
      { name: 'Outil newsletter', desc: 'Envoi des actualités par e-mail', status: 'À venir', tone: 'n', sync: '—', actionLabel: 'Bientôt' },
      { name: 'Paiement / adhésion en ligne', desc: 'Paiement de la cotisation en ligne', status: 'À venir', tone: 'n', sync: '—', actionLabel: 'Bientôt' },
      { name: 'Analytics', desc: 'Statistiques de fréquentation du site', status: 'À venir', tone: 'n', sync: '—', actionLabel: 'Bientôt' },
      { name: 'Réseaux sociaux', desc: 'Partage automatique sur Facebook et Instagram', status: 'À venir', tone: 'n', sync: '—', actionLabel: 'Bientôt' },
      { name: 'Formulaires', desc: 'Réception des formulaires du site (recensement/adhésion, contact)', status: 'Connecté', tone: 'g', sync: 'En continu', actionLabel: 'Voir' }
    ];

    const legalSections = [
      { title: 'Mentions légales', updated: '12 juillet 2026' },
      { title: 'Politique de confidentialité', updated: '12 juillet 2026' },
      { title: 'Gestion des cookies', updated: '5 mars 2026' },
      { title: 'Durées de conservation des données', updated: '5 mars 2026' },
      { title: 'Consentements des formulaires', updated: '5 mars 2026' }
    ];

    // Helper function for badges
    function getBadgeHtml(label, tone) {
      const map = {
        g: { bg: 'rgba(23,107,77,0.12)', color: '#176B4D' },
        o: { bg: 'rgba(233,120,36,0.16)', color: '#B75E12' },
        r: { bg: 'rgba(177,69,36,0.13)', color: '#B14524' },
        n: { bg: 'rgba(31,41,37,0.08)', color: '#5a655f' }
      };
      const c = map[tone] || map.n;
      return `<span style="background:${c.bg}; color:${c.color}; padding:4px 12px; border-radius:999px; font-size:12px; font-weight:700;">${label}</span>`;
    }

    document.addEventListener('DOMContentLoaded', () => {
      generateNav();

      // Câblage des vues partagées (upload, formulaires) — une seule fois.
      content.wire();
      document.querySelectorAll('.btn-create-news').forEach((btn) => {
        btn.addEventListener('click', () => content.resetNewsForm());
      });
      document.querySelectorAll('.btn-create-event').forEach((btn) => {
        btn.addEventListener('click', () => content.resetEventForm());
      });
      // Boutons « Annuler » / retour des formulaires partagés.
      document.querySelectorAll('.nav-trigger-btn').forEach((btn) => {
        btn.addEventListener('click', () => goPage(btn.dataset.page));
      });

      let initialPage = 'overview';
      try {
        const saved = sessionStorage.getItem('superadmin-current-page');
        if (saved && document.getElementById(`view-${saved}`)) initialPage = saved;
      } catch {}
      goPage(initialPage);
      loadSessionIdentity();

      document.querySelectorAll('.logout-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          window.location.href = '/connexion';
        });
      });

      // Mobile Drawer actions
      const openDrawerBtn = document.getElementById('open-mobile-drawer');
      const closeDrawerBtn = document.getElementById('close-mobile-drawer');
      const mobileDrawer = document.getElementById('mobile-sidebar-drawer');

      openDrawerBtn.addEventListener('click', () => {
        mobileDrawer.classList.remove('hidden');
      });
      closeDrawerBtn.addEventListener('click', () => {
        mobileDrawer.classList.add('hidden');
      });

      // Afficher/masquer un champ mot de passe (délégation : couvre tous
      // les boutons .toggle-password-visibility-btn, y compris ceux
      // injectés plus tard).
      document.addEventListener('click', (e) => {
        const btn = e.target.closest('.toggle-password-visibility-btn');
        if (!btn) return;
        const input = document.getElementById(btn.dataset.target);
        if (!input) return;
        const showing = input.type === 'text';
        input.type = showing ? 'password' : 'text';
        btn.setAttribute('aria-label', showing ? 'Afficher le mot de passe' : 'Masquer le mot de passe');
      });
    });

    // Identité de session : nom/rôle réels dans la sidebar, la salutation
    // de la Vue d'ensemble, et l'id nécessaire pour bloquer l'auto-suspension
    // dans Utilisateurs & rôles.
    async function loadSessionIdentity() {
      try {
        const res = await fetch('/api/superadmin/profile');
        const data = await res.json();
        if (!res.ok) return;
        state.currentUserId = data.id;
        const roleLabel = PROFILE_ROLE_LABELS[data.role] || data.role;
        document.getElementById('sidebar-user-name').textContent = data.nom;
        document.getElementById('sidebar-user-role').textContent = roleLabel;
      } catch {
        // Silencieux : la sidebar garde ses tirets par défaut.
      }
    }

    function goPage(pageKey) {
      state.page = pageKey;
      
      document.querySelectorAll('.sidebar-link').forEach(link => {
        if (link.dataset.page === pageKey) {
          link.classList.add('active-nav-btn');
        } else {
          link.classList.remove('active-nav-btn');
        }
      });

      const pageTitleMap = {
        overview: "Vue d'ensemble", users: 'Utilisateurs & rôles', contenus: 'Contenus & validations',
        donnees: 'Données & exports', journal: "Journal d'activité", reglages: 'Réglages du site',
        integrations: 'Intégrations', securite: 'Sécurité', sauvegardes: 'Sauvegardes',
        mentions: 'Mentions & RGPD', stats: 'Statistiques globales', profil: 'Mon profil',
        actualites: 'Actualités', 'actualites-new': 'Nouvelle actualité',
        evenements: 'Événements', 'evenements-new': 'Créer un événement',
        inscriptions: 'Inscriptions',
        messages: 'Messages'
      };

      document.getElementById('breadcrumb-page-label').textContent = pageTitleMap[pageKey] || '';
      document.getElementById('title-page-label').textContent = pageTitleMap[pageKey] || '';

      document.querySelectorAll('section[id^="view-"]').forEach(sec => {
        sec.classList.add('hidden');
      });
      
      const targetView = document.getElementById(`view-${pageKey}`);
      if (targetView) {
        targetView.classList.remove('hidden');
        // Retient l'onglet courant pour qu'un rechargement de page (F5) y
        // revienne au lieu de repartir sur Vue d'ensemble.
        try { sessionStorage.setItem('superadmin-current-page', pageKey); } catch {}
      }

      document.getElementById('mobile-sidebar-drawer').classList.add('hidden');

      renderPageDetails(pageKey);
    }

    function generateNav() {
      const desktopContainer = document.getElementById('desktop-sidebar-nav');
      const mobileContainer = document.getElementById('mobile-sidebar-nav');

      desktopContainer.innerHTML = '';
      mobileContainer.innerHTML = '';

      function createLink(item) {
        const link = document.createElement('a');
        link.className = 'sidebar-link';
        link.dataset.page = item.key;
        link.style.cursor = 'pointer';
        const svgContent = svgMap[item.key] || '';
        link.innerHTML = `${svgContent}<span>${item.label}</span>`;
        link.addEventListener('click', () => {
          goPage(item.key);
        });
        return link;
      }

      navigationItems.forEach(item => {
        if (item.type === 'header') {
          const header = document.createElement('div');
          header.style.cssText = 'font-size:11px; font-weight:700; color:#9aa39c; text-transform:uppercase; letter-spacing:0.05em; padding:16px 14px 6px;';
          header.textContent = item.label;
          desktopContainer.appendChild(header);
          mobileContainer.appendChild(header.cloneNode(true));
        } else if (item.type === 'separator') {
          const sep = document.createElement('div');
          sep.style.cssText = 'height:1px; background:rgba(31,41,37,0.07); margin:10px 6px;';
          desktopContainer.appendChild(sep);
          mobileContainer.appendChild(sep.cloneNode(true));
        } else {
          desktopContainer.appendChild(createLink(item));
          mobileContainer.appendChild(createLink(item));
        }
      });
    }

    function logActivity(user, role, action, detail) {
      const date = new Date();
      const pad = (n) => String(n).padStart(2, '0');
      const dateStr = `${date.getDate()} juillet 2026, ${pad(date.getHours())}:${pad(date.getMinutes())}`;
      
      journalLogs.unshift({
        date: dateStr,
        user,
        role,
        roleT: 'g',
        action,
        detail,
        ip: '82.1XX.XX.XX'
      });
    }

    function renderPageDetails(pageKey) {
      if (pageKey === 'overview') {
        dashboard.load();
      } else if (pageKey === 'users') {
        loadUsers();
      } else if (pageKey === 'contenus') {
        loadValidations();
      } else if (pageKey === 'journal') {
        loadJournal();
      } else if (pageKey === 'reglages') {
        loadSettings();
      } else if (pageKey === 'integrations') {
        renderIntegrations();
      } else if (pageKey === 'securite') {
        renderSecurite();
      } else if (pageKey === 'mentions') {
        renderMentions();
      } else if (pageKey === 'stats') {
        loadStats();
      } else if (pageKey === 'profil') {
        loadProfile();
      } else if (pageKey === 'actualites') {
        loadContentThen(content.renderActualites);
      } else if (pageKey === 'evenements') {
        loadContentThen(content.renderEvenements);
      } else if (pageKey === 'inscriptions') {
        loadContentThen(content.renderInscriptions);
      } else if (pageKey === 'messages') {
        loadContentThen(content.renderMessages);
      }
    }

    // Les vues partagées ont besoin des données de /api/admin/data. On les
    // charge à la première ouverture seulement, puis on rejoue le rendu.
    let contentDataLoaded = false;
    async function loadContentThen(render) {
      if (contentDataLoaded) {
        render();
        return;
      }
      try {
        content.setData(await content.fetchData());
        contentDataLoaded = true;
        render();
      } catch (err) {
        alert('Impossible de charger les contenus : ' + err.message);
      }
    }

    // 1. Overview — vue partagée, voir lib/dashboardCore.js
    // (dashboard.load(), appelée depuis renderPageDetails()).

    // 2. Users
    function renderUsers() {
      const container = document.getElementById('users-list-container');
      container.innerHTML = '';

      users.forEach((u, i) => {
        const isSelected = state.userSelectedIdx === i;
        const item = document.createElement('div');
        item.style.cssText = `display:flex; align-items:center; gap:14px; padding:14px 16px; border-bottom:1px solid rgba(31,41,37,0.06); cursor:pointer; flex-wrap:wrap; background:${isSelected ? '#F8F4EC' : 'transparent'}; border-left:3px solid ${isSelected ? '#176B4D' : 'transparent'};`;
        item.innerHTML = `
          <div style="width:36px; height:36px; border-radius:50%; background:${u.avatarBg}; flex-shrink:0;"></div>
          <div style="flex:1; min-width:160px;">
            <div style="font-size:14px; font-weight:700; color:#1F2925;">${u.name}</div>
            <div style="font-size:12.5px; color:#5a655f;">${u.email}</div>
          </div>
          ${getBadgeHtml(u.role, u.roleT)}
          ${getBadgeHtml(u.status, u.statusT)}
          <div style="font-size:12px; color:#9aa39c; min-width:120px;">Dernière con. : ${u.last}</div>
        `;
        item.addEventListener('click', () => {
          state.userSelectedIdx = i;
          renderUsers();
        });
        container.appendChild(item);
      });

      const detailBox = document.getElementById('user-detail-box');
      const selected = users[state.userSelectedIdx];

      if (!selected) {
        detailBox.innerHTML = '';
        return;
      }

      detailBox.innerHTML = `
        <div style="display:flex; align-items:center; gap:14px; margin-bottom:18px;">
          <div style="width:48px; height:48px; border-radius:50%; background:${selected.avatarBg};"></div>
          <div>
            <div style="font-size:16px; font-weight:700; color:#1F2925;">${selected.name}</div>
            <div style="font-size:13px; color:#5a655f;">${selected.email}</div>
          </div>
        </div>
        <div style="display:flex; gap:8px; margin-bottom:18px;">
          ${getBadgeHtml(selected.role, selected.roleT)}
          ${getBadgeHtml(selected.status, selected.statusT)}
        </div>
        <div style="font-size:13px; color:#5a655f; margin-bottom:6px;">Dernière connexion : ${selected.last}</div>
        <div style="font-size:13px; font-weight:700; color:#1F2925; margin:16px 0 8px;">Permissions détaillées</div>
        <p style="font-size:13.5px; line-height:1.6; color:#3f4a45; margin:0 0 20px; background: rgba(31,41,37,0.02); padding: 10px; border-radius:8px;">${selected.permissions}</p>
        <div style="display:flex; flex-direction:column; gap:10px;">
          <button id="user-reset-btn" style="background:#F8F4EC; color:#1F2925; border:none; padding:11px; border-radius:999px; font-weight:700; font-size:13.5px; cursor:pointer;">Réinitialiser l'accès</button>
          ${selected.id === state.currentUserId
            ? `<div style="font-size:12.5px; color:#9aa39c; text-align:center; padding:8px;">C'est votre propre compte — utilisez « Mon profil » pour le modifier. Vous ne pouvez pas vous suspendre ni vous supprimer vous-même.</div>`
            : `<button id="user-toggle-btn" style="background:#F8F4EC; color:#1F2925; border:none; padding:11px; border-radius:999px; font-weight:700; font-size:13.5px; cursor:pointer;">${selected.status === 'Actif' ? 'Suspendre le compte' : 'Activer le compte'}</button>
               <button id="user-delete-btn" style="background:rgba(177,69,36,0.08); color:#B14524; border:none; padding:11px; border-radius:999px; font-weight:700; font-size:13.5px; cursor:pointer;">Supprimer l'utilisateur</button>`}
        </div>
      `;

      document.getElementById('user-reset-btn').addEventListener('click', async () => {
        try {
          const res = await fetch('/api/superadmin/users', {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ id: selected.id, resetPassword: true })
          });
          const data = await res.json();
          if (!res.ok) throw new Error(data.error || 'Erreur.');
          alert(`Nouveau mot de passe temporaire pour ${selected.email} : ${data.tempPassword}\n(à communiquer manuellement — aucun e-mail n'est envoyé)`);
        } catch (err) {
          alert(err.message);
        }
      });
      document.getElementById('user-toggle-btn')?.addEventListener('click', async () => {
        const newStatut = selected.dbStatut === 'actif' ? 'desactive' : 'actif';
        try {
          const res = await fetch('/api/superadmin/users', {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ id: selected.id, statut: newStatut })
          });
          const data = await res.json();
          if (!res.ok) throw new Error(data.error || 'Erreur.');
          state.userSelectedIdx = users.findIndex(u => u.id === selected.id);
          await loadUsers();
        } catch (err) {
          alert(err.message);
        }
      });
      document.getElementById('user-delete-btn')?.addEventListener('click', async () => {
        if (!confirm(`Supprimer définitivement le compte de ${selected.name} (${selected.email}) ? Cette action est irréversible.`)) return;
        try {
          const res = await fetch(`/api/superadmin/users?id=${selected.id}`, { method: 'DELETE' });
          const data = await res.json();
          if (!res.ok) throw new Error(data.error || 'Erreur.');
          state.userSelectedIdx = 0;
          await loadUsers();
        } catch (err) {
          alert(err.message);
        }
      });

      const matrixWrapper = document.getElementById('permission-matrix-rows');
      matrixWrapper.innerHTML = '';
      permissionMatrix.forEach(row => {
        const tr = document.createElement('div');
        tr.style.cssText = 'display:grid; grid-template-columns:1.6fr 1fr 1fr 1fr; gap:8px; padding:10px 0; border-bottom:1px solid rgba(31,41,37,0.05); align-items:center;';
        tr.innerHTML = `
          <div style="font-size:13px; color:#1F2925;">${row.label}</div>
          <div style="text-align:center; font-size:15px; font-weight:600; color:#176B4D;">${row.superadmin}</div>
          <div style="text-align:center; font-size:15px; font-weight:600; color:#E97824;">${row.admin}</div>
          <div style="text-align:center; font-size:15px; font-weight:600; color:#5a655f;">${row.editeur}</div>
        `;
        matrixWrapper.appendChild(tr);
      });
    }

    const toggleInviteBtn = document.getElementById('toggle-invite-btn');
    const inviteBox = document.getElementById('invite-user-box');
    toggleInviteBtn.addEventListener('click', () => {
      state.inviteOpen = !state.inviteOpen;
      if (state.inviteOpen) {
        inviteBox.classList.remove('hidden');
      } else {
        inviteBox.classList.add('hidden');
      }
      document.getElementById('invite-success-alert').classList.add('hidden');
    });

    document.getElementById('invite-user-submit-btn').addEventListener('click', async () => {
      const name = document.getElementById('invite-form-name').value.trim();
      const email = document.getElementById('invite-form-email').value.trim();
      const roleSelectValue = document.getElementById('invite-form-role').value;
      const dbRoleMap = { Admin: 'admin', 'Super Admin': 'super_admin', 'Éditeur / Bénévole': 'editeur' };

      if (!name || !email) {
        alert('Veuillez remplir le nom et le mail.');
        return;
      }

      try {
        const res = await fetch('/api/superadmin/users', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ nom: name, email, role: dbRoleMap[roleSelectValue] })
        });
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur.');

        const successAlert = document.getElementById('invite-success-alert');
        successAlert.textContent = `✓ Compte créé pour ${email}. Mot de passe temporaire : ${data.tempPassword} (à communiquer manuellement, aucun e-mail n'est envoyé).`;
        successAlert.classList.remove('hidden');

        document.getElementById('invite-form-name').value = '';
        document.getElementById('invite-form-email').value = '';
        await loadUsers();
      } catch (err) {
        alert(err.message);
      }
    });

    // 3. Validations
    function renderValidations() {
      const columns = document.getElementById('validations-columns');
      const emptyState = document.getElementById('validations-empty-state');
      const isEmpty = validationQueue.length === 0;
      columns.classList.toggle('hidden', isEmpty);
      emptyState.classList.toggle('hidden', !isEmpty);
      if (isEmpty) return;

      const listWrapper = document.getElementById('validations-queue-list');
      listWrapper.innerHTML = '';

      validationQueue.forEach((c, i) => {
        const isSelected = state.validationSelectedIdx === i;
        const item = document.createElement('div');
        item.style.cssText = `padding:16px; border-bottom:1px solid rgba(31,41,37,0.06); cursor:pointer; background:${isSelected ? '#F8F4EC' : 'transparent'}; border-left:3px solid ${isSelected ? '#176B4D' : 'transparent'};`;
        item.innerHTML = `
          <div style="display:flex; justify-content:space-between; gap:8px;">
            <span style="background:rgba(23,107,77,0.08); color:#176B4D; padding:3px 10px; border-radius:999px; font-size:11px; font-weight:700;">${c.type}</span>
            <span style="font-size:12px; color:#9aa39c;">${c.date}</span>
          </div>
          <div style="font-size:14px; font-weight:700; color:#1F2925; margin-top:8px;">${c.title}</div>
          <div style="font-size:12.5px; color:#5a655f; margin-top:2px;">Par ${c.author} · ${c.status}</div>
        `;
        item.addEventListener('click', () => {
          state.validationSelectedIdx = i;
          renderValidations();
        });
        listWrapper.appendChild(item);
      });

      const detailBox = document.getElementById('validation-detail-box');
      const selected = validationQueue[state.validationSelectedIdx] || validationQueue[0];

      if (!selected) {
        detailBox.innerHTML = '';
        return;
      }

      detailBox.innerHTML = `
        <h3 style="font-size:16px; font-weight:700; color:#1F2925; margin:0 0 6px;">${selected.title}</h3>
        <div style="font-size:13px; color:#5a655f; margin-bottom:16px;">Par ${selected.author} · ${selected.date}</div>
        <div style="aspect-ratio:16/9; border-radius:14px; background:linear-gradient(150deg,#E8D8BF,#176B4D); margin-bottom:18px;"></div>
        <div style="font-size:13px; font-weight:700; color:#1F2925; margin-bottom:8px;">Commentaire pour l'auteur</div>
        <textarea id="validation-comment-text" rows="2" style="width:100%; padding:12px 14px; border-radius:10px; border:1.5px solid #e3dccb; font-size:13.5px; resize:vertical; margin-bottom:16px;" placeholder="Optionnel..."></textarea>
        <div style="display:flex; gap:10px; flex-wrap:wrap;">
          <button id="validation-approve-btn" style="background:#176B4D; color:#FFFFFF; border:none; padding:11px 20px; border-radius:999px; font-weight:700; font-size:13.5px; cursor:pointer;">Approuver et publier</button>
          <button id="validation-return-btn" style="background:#F8F4EC; color:#1F2925; border:none; padding:11px 20px; border-radius:999px; font-weight:700; font-size:13.5px; cursor:pointer;">Renvoyer avec commentaire</button>
        </div>
      `;

      document.getElementById('validation-approve-btn').addEventListener('click', async () => {
        try {
          const res = await fetch('/api/superadmin/validations', {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ id: selected.id, action: 'approve' })
          });
          const data = await res.json();
          if (!res.ok) throw new Error(data.error || 'Erreur.');
          alert(`Le contenu "${selected.title}" a été approuvé et publié !`);
          await loadValidations();
        } catch (err) {
          alert(err.message);
        }
      });

      document.getElementById('validation-return-btn').addEventListener('click', async () => {
        const comment = document.getElementById('validation-comment-text').value.trim() || 'Des ajustements sont requis.';
        try {
          const res = await fetch('/api/superadmin/validations', {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ id: selected.id, action: 'return', comment })
          });
          const data = await res.json();
          if (!res.ok) throw new Error(data.error || 'Erreur.');
          alert(`Le contenu a été renvoyé avec votre commentaire.`);
          await loadValidations();
        } catch (err) {
          alert(err.message);
        }
      });
    }

    const generateExportBtn = document.getElementById('generate-export-btn');
    const exportConfirmBox = document.getElementById('export-confirm-box');
    const exportSuccessAlert = document.getElementById('export-success-alert');

    generateExportBtn.addEventListener('click', () => {
      const reason = document.getElementById('export-reason').value.trim();
      if (!reason) {
        alert('Veuillez entrer une raison de sécurité pour justifier cet export.');
        return;
      }
      exportConfirmBox.classList.remove('hidden');
      exportSuccessAlert.classList.add('hidden');
    });

    document.getElementById('confirm-export-action').addEventListener('click', async () => {
      const reason = document.getElementById('export-reason').value.trim();
      const period = document.getElementById('export-period').value;
      const include = ['recensement', 'inscriptions', 'messages'].filter(
        (k) => document.getElementById(`export-check-${k}`).checked
      );

      if (include.length === 0) {
        alert('Sélectionnez au moins une source de données.');
        return;
      }

      try {
        const res = await fetch('/api/superadmin/export', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ include, period, reason })
        });
        if (!res.ok) {
          const data = await res.json();
          throw new Error(data.error || 'Erreur lors de la génération de l\'export.');
        }
        const csv = await res.text();
        const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = `export-anb-${Date.now()}.csv`;
        document.body.appendChild(a);
        a.click();
        a.remove();
        URL.revokeObjectURL(url);

        exportConfirmBox.classList.add('hidden');
        exportSuccessAlert.classList.remove('hidden');
        setTimeout(() => {
          exportSuccessAlert.classList.add('hidden');
          document.getElementById('export-reason').value = '';
        }, 3000);
      } catch (err) {
        alert(err.message);
      }
    });

    document.getElementById('cancel-export-action').addEventListener('click', () => {
      exportConfirmBox.classList.add('hidden');
    });

    // 5. Journal
    function renderJournal() {
      const tabsWrapper = document.querySelector('.journal-tab-container');
      tabsWrapper.innerHTML = '';
      const filters = ['Tous', ...new Set(journalLogs.map(j => j.user))];

      filters.forEach(f => {
        const btn = document.createElement('button');
        btn.style.cssText = `border:2px solid #176B4D; padding:9px 18px; border-radius:999px; font-weight:700; font-size:13.5px; cursor:pointer;`;
        if (state.journalUserFilter === f) {
          btn.style.background = '#176B4D';
          btn.style.color = '#FFFFFF';
        } else {
          btn.style.background = 'transparent';
          btn.style.color = '#176B4D';
        }
        btn.textContent = f;
        btn.addEventListener('click', () => {
          state.journalUserFilter = f;
          renderJournal();
        });
        tabsWrapper.appendChild(btn);
      });

      const container = document.getElementById('journal-list-container');
      container.innerHTML = '';

      const filtered = state.journalUserFilter === 'Tous' ? journalLogs : journalLogs.filter(j => j.user === state.journalUserFilter);

      filtered.forEach(j => {
        const row = document.createElement('div');
        row.style.cssText = 'display:flex; gap:16px; padding:16px; border-bottom:1px solid rgba(31,41,37,0.06); flex-wrap:wrap; font-size: 13.5px;';
        row.innerHTML = `
          <div style="min-width:170px; color:#5a655f;">${j.date}</div>
          <div style="min-width:110px; font-weight:700; color:#1F2925;">${j.user}</div>
          <div style="min-width:120px;">${getBadgeHtml(j.role, j.roleT)}</div>
          <div style="flex:1; min-width:220px; color:#1F2925;"><strong>${j.action}</strong> — <span style="color:#5a655f;">${j.detail}</span></div>
          <div style="font-size:11.5px; color:#9aa39c;">IP ${j.ip}</div>
        `;
        container.appendChild(row);
      });
    }

    // 5bis. Réglages du site (identité + SEO -> site_settings)
    async function loadSettings() {
      try {
        const res = await fetch('/api/superadmin/settings');
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur de chargement.');
        const s = data.settings;
        document.getElementById('settings-identite-nom').value = s.identite_nom || '';
        document.getElementById('settings-identite-slogan').value = s.identite_slogan || '';
        document.getElementById('settings-identite-email').value = s.identite_email || '';
        document.getElementById('settings-identite-telephone').value = s.identite_telephone || '';
        document.getElementById('settings-seo-titre').value = s.seo_titre || '';
        document.getElementById('settings-seo-description').value = s.seo_description || '';
      } catch (err) {
        alert(err.message);
      }
    }

    async function saveSettings(keys) {
      const payload = {};
      keys.forEach((k) => { payload[k] = document.getElementById(`settings-${k.replace(/_/g, '-')}`).value.trim(); });
      try {
        const res = await fetch('/api/superadmin/settings', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur.');
        alert('Réglages enregistrés.');
      } catch (err) {
        alert(err.message);
      }
    }

    document.getElementById('save-identite-btn').addEventListener('click', () => {
      saveSettings(['identite_nom', 'identite_slogan', 'identite_email', 'identite_telephone']);
    });
    document.getElementById('save-seo-btn').addEventListener('click', () => {
      saveSettings(['seo_titre', 'seo_description']);
    });

    // 5ter. Mon profil
    const PROFILE_ROLE_LABELS = { admin: 'Admin', super_admin: 'Super Admin', editeur: 'Éditeur' };

    async function loadProfile() {
      try {
        const res = await fetch('/api/superadmin/profile');
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur de chargement.');
        const roleLabel = PROFILE_ROLE_LABELS[data.role] || data.role;
        document.getElementById('profile-form-name').value = data.nom;
        document.getElementById('profile-form-email').value = data.email;
        document.getElementById('profile-form-role').value = roleLabel;
        document.getElementById('profile-form-password').value = '';
        document.getElementById('profile-form-password-confirm').value = '';
        document.getElementById('profile-header-name').textContent = data.nom;
        document.getElementById('profile-header-role').textContent = roleLabel;
      } catch (err) {
        alert(err.message);
      }
    }

    document.getElementById('save-profile-btn').addEventListener('click', async () => {
      const nom = document.getElementById('profile-form-name').value.trim();
      const email = document.getElementById('profile-form-email').value.trim();
      const password = document.getElementById('profile-form-password').value;
      const passwordConfirm = document.getElementById('profile-form-password-confirm').value;

      if (password && password !== passwordConfirm) {
        alert('Les deux mots de passe ne correspondent pas.');
        return;
      }

      try {
        const res = await fetch('/api/superadmin/profile', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ nom, email, password: password || undefined })
        });
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur.');
        alert('Profil enregistré !');
        document.getElementById('profile-form-password').value = '';
        document.getElementById('profile-form-password-confirm').value = '';
        document.getElementById('profile-header-name').textContent = nom;
        document.getElementById('sidebar-user-name').textContent = nom;
      } catch (err) {
        alert(err.message);
      }
    });

    // 6. Intégrations
    function renderIntegrations() {
      const container = document.getElementById('integrations-list-container');
      container.innerHTML = '';

      integrations.forEach(i => {
        const card = document.createElement('div');
        card.style.cssText = 'background:#FFFFFF; border-radius:18px; padding:22px; box-shadow:0 8px 24px rgba(31,41,37,0.05); display: flex; flex-direction: column; justify-content: space-between;';
        card.innerHTML = `
          <div>
            <div style="display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:10px; gap: 8px;">
              <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0;">${i.name}</h3>
              ${getBadgeHtml(i.status, i.tone)}
            </div>
            <p style="font-size:13px; color:#5a655f; line-height:1.5; margin:0 0 12px;">${i.desc}</p>
            <div style="font-size:11.5px; color:#9aa39c; margin-bottom:14px;">Synchro : ${i.sync}</div>
          </div>
          <button style="background:#F8F4EC; color:#1F2925; border:none; padding:9px 18px; border-radius:999px; font-weight:700; font-size:12.5px; cursor:pointer;" onclick="alert('Configuration integration : ${i.name}')">${i.actionLabel}</button>
        `;
        container.appendChild(card);
      });
    }

    // 7. Sécurité
    function renderSecurite() {
      const toggleBtn = document.getElementById('twofa-toggle-btn');
      const indicator = document.getElementById('twofa-toggle-indicator');
      const statusLabel = document.getElementById('twofa-status-label');

      const update2FA = () => {
        if (state.twoFA) {
          toggleBtn.style.background = '#176B4D';
          indicator.style.left = '23px';
          statusLabel.textContent = 'Activée pour votre compte.';
        } else {
          toggleBtn.style.background = '#d8d0c0';
          indicator.style.left = '3px';
          statusLabel.textContent = 'Désactivée — activation recommandée.';
        }
      };

      toggleBtn.replaceWith(toggleBtn.cloneNode(true));
      document.getElementById('twofa-toggle-btn').addEventListener('click', () => {
        state.twoFA = !state.twoFA;
        update2FA();
        logActivity('Nasser', 'Super Admin', 'Sécurité 2FA modifiée', state.twoFA ? '2FA Activée' : '2FA Désactivée');
      });

      update2FA();

      const list = document.getElementById('active-sessions-list');
      list.innerHTML = '';
      const sessions = [
        { device: 'Chrome — Windows (Garba)', location: 'Bordeaux, France', time: 'Session active' },
        { device: 'Safari — iPhone', location: 'Bordeaux, France', time: 'Il y a 1 heure' },
        { device: 'Chrome — Windows (Test)', location: 'Paris, France', time: 'Il y a 2 jours' }
      ];

      sessions.forEach(s => {
        const item = document.createElement('div');
        item.style.cssText = 'display:flex; justify-content:space-between; padding:10px 0; border-bottom:1px solid rgba(31,41,37,0.06); font-size:13.5px; flex-wrap:wrap; gap:8px;';
        item.innerHTML = `
          <span style="color:#1F2925; font-weight:600;">${s.device}</span>
          <span style="color:#5a655f;">${s.location}</span>
          <span style="color:#9aa39c;">${s.time}</span>
        `;
        list.appendChild(item);
      });

      document.getElementById('disconnect-all-btn').onclick = () => {
        alert('Toutes les autres sessions ont été déconnectées.');
        logActivity('Nasser', 'Super Admin', 'Déconnexion sessions actives', 'Toutes sessions tierces révoquées');
      };
    }

    // 9. Mentions
    function renderMentions() {
      const container = document.getElementById('legal-sections-container');
      container.innerHTML = '';

      legalSections.forEach(ls => {
        const item = document.createElement('div');
        item.style.cssText = 'background:#FFFFFF; border-radius:18px; padding:22px; box-shadow:0 8px 24px rgba(31,41,37,0.05); display: flex; justify-content: space-between; align-items: center; gap: 14px;';
        item.innerHTML = `
          <div>
            <h3 style="font-size:15px; font-weight:700; color:#1F2925; margin:0 0 6px;">${ls.title}</h3>
            <div style="font-size:12.5px; color:#9aa39c;">Dernière modification : ${ls.updated}</div>
          </div>
          <button style="background:#F8F4EC; color:#1F2925; border:none; padding:8px 16px; border-radius:999px; font-weight:700; font-size:12.5px; cursor:pointer;" onclick="alert('Modifier : ${ls.title}')">Modifier</button>
        `;
        container.appendChild(item);
      });
    }

    // 10. Statistiques
    const STATS_PERIODS = [
      { label: '7 jours', days: '7' },
      { label: '30 jours', days: '30' },
      { label: '3 mois', days: '90' },
      { label: '1 an', days: '365' }
    ];

    function renderStatsPeriodButtons() {
      const periodsWrapper = document.querySelector('.stats-period-container');
      periodsWrapper.innerHTML = '';
      STATS_PERIODS.forEach(p => {
        const btn = document.createElement('button');
        btn.style.cssText = `border:2px solid #176B4D; padding:8px 16px; border-radius:999px; font-weight:700; font-size:13px; cursor:pointer;`;
        if (state.statsPeriod === p.label) {
          btn.style.background = '#176B4D';
          btn.style.color = '#FFFFFF';
        } else {
          btn.style.background = 'transparent';
          btn.style.color = '#176B4D';
        }
        btn.textContent = p.label;
        btn.addEventListener('click', () => {
          state.statsPeriod = p.label;
          loadStats();
        });
        periodsWrapper.appendChild(btn);
      });
    }

    async function loadStats() {
      renderStatsPeriodButtons();
      const periodDef = STATS_PERIODS.find(p => p.label === state.statsPeriod) || STATS_PERIODS[1];
      try {
        const res = await fetch(`/api/superadmin/stats?period=${periodDef.days}`);
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Erreur de chargement.');

        // Total (pas filtré par période) : même source que le dashboard
        // Admin — voir lib/stats.js. Cette carte n'a jamais été un vrai
        // "sur la période" malgré sa place ici (son libellé ne l'indiquait
        // pas), donc pas de changement de comportement visible attendu
        // hors correction de cohérence.
        document.getElementById('stat-period-inscriptions').textContent = data.overview.inscriptions_evenements;
        document.getElementById('stat-period-adhesions').textContent = data.period_stats.adhesions;
        document.getElementById('stat-period-membres').textContent = data.overview.membres;
        document.getElementById('stat-period-evenements').textContent = data.overview.evenements_a_venir;

        const eventsList = document.getElementById('top-pages-list');
        eventsList.innerHTML = '';
        if (data.upcoming_events.length === 0) {
          eventsList.innerHTML = '<div style="font-size:13.5px; color:#5a655f;">Aucun événement à venir.</div>';
        }
        data.upcoming_events.forEach(ev => {
          const item = document.createElement('div');
          item.style.cssText = 'display:flex; justify-content:space-between; padding:9px 0; border-bottom:1px solid rgba(31,41,37,0.06); font-size:13.5px; gap:10px;';
          item.innerHTML = `<span style="color:#1F2925;">${ev.title}</span><span style="font-weight:700; color:#176B4D; white-space:nowrap;">${ev.date}</span>`;
          eventsList.appendChild(item);
        });

        const articlesList = document.getElementById('top-articles-list');
        articlesList.innerHTML = '';
        if (data.latest_articles.length === 0) {
          articlesList.innerHTML = '<div style="font-size:13.5px; color:#5a655f;">Aucun article.</div>';
        }
        data.latest_articles.forEach(a => {
          const item = document.createElement('div');
          item.style.cssText = 'display:flex; justify-content:space-between; padding:9px 0; border-bottom:1px solid rgba(31,41,37,0.06); font-size:13.5px; gap:10px;';
          item.innerHTML = `<span style="color:#1F2925;">${a.title}</span>${getBadgeHtml(a.status, a.status === 'Publié' ? 'g' : 'n')}`;
          articlesList.appendChild(item);
        });
      } catch (err) {
        document.getElementById('top-pages-list').innerHTML = `<div style="font-size:13.5px; color:#B14524;">${err.message}</div>`;
      }
    }
  </script>

</body>
</html>
